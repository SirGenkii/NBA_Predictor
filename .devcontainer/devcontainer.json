{
  "name": "Jupyter GPU",
  "context": "..",
  "dockerFile": "../Dockerfile",
  "runArgs": ["--gpus", "all"],
  "workspaceFolder": "/workspace",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "extensions": [
    "ms-python.python",
    "ms-toolsai.jupyter"
  ]
}
